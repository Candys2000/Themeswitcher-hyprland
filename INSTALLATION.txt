copy this into your terminal, each line individually or run the installation.sh:

sudo pacman -S waybar grim xdg-desktop-portal-hyprland xdg-desktop-portal-gtk wl-clipboard dolphin breeze-icons ttf-jetbrains-mono-nerd ffmpegthumbs kdegraphics-thumbnailers archlinux-xdg-menu brightnessctl swaybg hypridle hyprlock

yay hyprpolkitagent xwaylandbridge xorg-wayland wpgtk

sudo pacman -S --needed base-devel git

git clone https://aur.archlinux.org/wpgtk.git

cd ~/wpgtk_install && makepkg -si

wpg-install.sh -g

wpg

//wpg should open now, close it again

sudo pacman -S --needed git base-devel

git clone https://aur.archlinux.org/yay.git

cd yay makepkg -si

yay -S hyprlauncher quickshell nwg-drawer

//now you can just copy my config files into the home and .config folder.
